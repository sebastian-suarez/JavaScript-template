# JavaScript template 👋

A clean and friendly starter for Node.js projects using modern JavaScript.

## ✨ What You Get

- ES modules (`"type": "module"`)
- Import aliases via `imports` (`#src/*`, `#modules/*`)
- Jest for tests
- XO + Prettier for linting and formatting
- Husky for Git hooks

## 📦 Requirements

- Node.js 18+

## 🚀 Quick Start

```bash
npm install
npm start
```

## 🧰 Available Scripts

- `npm start` Start the example app (`src/index.js`)
- `npm test` Run the test suite with Jest
- `npm run lint` Check code style with XO
- `npm run format` Format files with Prettier

## 📁 Project Structure

- `src/index.js` Main entry point
- `src/modules/` Reusable modules
- `tests/` Automated tests

## ✅ Great For

- Bootstrapping new Node.js apps quickly
- Keeping code quality high from day one
- Starting with sensible defaults instead of a blank folder
